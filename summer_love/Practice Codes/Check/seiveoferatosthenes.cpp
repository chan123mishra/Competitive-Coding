#include<bits/stdc++.h>
using namespace std;
int main()
{

    int n;
    cin>>n;
    int flag[10000];
    memset(flag,0,sizeof(flag));
    for(int i = 2; i < n; i++)
    {
        if(flag[i] == 0)
    }
}
